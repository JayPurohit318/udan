const API_PREFIX = '/api';
const DEFAULT_PORT = 5000;

const RESPONSE_MESSAGES = {
  TEST_ROUTE_WORKING: 'Test route working',
  INTERNAL_SERVER_ERROR: 'Internal server error',
};

module.exports = {
  API_PREFIX,
  DEFAULT_PORT,
  RESPONSE_MESSAGES,
};
